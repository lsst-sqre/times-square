### Other changes

- The GitHub check-run notebook-execution failure reporting now derives its annotation titles and messages from the shared execution-outcome formatter (`timessquare.domain.executionoutcome`) rather than hand-branching on Noteburst error codes. This removes duplicated failure-description logic so the check-run annotations and the API `execution_error` field cannot drift apart. There is no user-visible change to what check runs report.
